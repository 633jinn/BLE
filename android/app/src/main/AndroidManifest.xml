<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 네트워크 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- BLE 권한들 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" 
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" 
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 11 이하 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- BLE 기능 선언 -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />


    <application
        android:name=".BleTransferApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BLE"
        tools:targetApi="24">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".AccountSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.BLE"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".TransferAmountActivity"
            android:exported="false"
            android:theme="@style/Theme.BLE"
            android:parentActivityName=".AccountSelectionActivity" />
            
        <activity
            android:name=".TransferConfirmationActivity"
            android:exported="false"
            android:theme="@style/Theme.BLE"
            android:parentActivityName=".TransferAmountActivity" />
            
        <activity
            android:name=".TransferResultActivity"
            android:exported="false"
            android:theme="@style/Theme.BLE"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".AccountAdditionActivity"
            android:exported="false"
            android:theme="@style/Theme.BLE"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>